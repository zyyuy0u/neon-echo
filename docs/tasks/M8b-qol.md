# M8b 派工單：AAA 對標——探索 QoL（P1）

## GOAL（目標與背景）
依 `docs/AAA-BENCHMARK.md`（先讀）之 M8b 批次。基線：M8a 已簽收（全測試綠）。

實作內容：
1. **羅盤條 HUD（原神式）** `src/ui/CompassBar.ts` + 純邏輯 `src/systems/compass/`：
   - 頂部橫條：N/E/S/W 基準刻度 + 圖示投影（依玩家 yaw 與目標方位角計算橫向位置，
     視野範圍 ±110°，超出漸隱於兩端）。**方位投影計算為純函式並單元測試**。
   - 圖示：四大地標（恆顯）+ 聖所（僅已「發現」——首次進入該區後標記入存檔）+
     北核心（三能力齊後顯示）。圖示下方顯示距離（公尺，>1000 顯示 km 一位小數）。
   - 與 HUD 淡出邏輯一致；減少動態=無滑動動畫但功能保留。
2. **快速傳送（薩爾達式）**：
   - 錨點：靜滯廣場（永遠）+ 各聖所（完成後）。資格計算純函式+測試。
   - 暫停選單新增「傳送」頁：錨點清單（未解鎖顯示為 ???）、鍵盤可操作、確認後
     0.8s 光柱淡出→傳送→淡入（減少動態=0.2s 純淡入淡出）。
   - 傳送落點 = 錨點安全座標（資料檔新增 `warpPoint` 欄位）；傳送寫存檔。
3. **聖所完成 fanfare**：完成謎題瞬間——輸入凍結 1.2s + 祭壇光柱粒子 + 上行琶音
   sting（AudioSystem 新事件）+ HUD 能力圖示放大點亮動畫。減少動態=僅 sting+圖示點亮。
   凍結期間 ESC 不可開選單（防狀態衝突）。
4. **UI 音效**：選單焦點移動（短 tick）、確認（上行雙音）、返回（下行雙音）、
   碎片計數 tick（拾取時）。走既有 AudioSystem，音量隨主音量。

## REDLINES（紅線）
- locomotion 手感與物理不准動；世界圖結構（graph.ts 節點/邊）不准動
  （zone 資料檔**允許新增** warpPoint/發現半徑欄位）。
- 既有測試斷言不准改弱；a11y 不倒退；不准新增依賴；不准 commit；不准跑 `npm run e2e`。

## ACCEPTANCE（驗收條件）
1. `npm run lint`、`npm run typecheck`、`npm test`、`npm run build` 全部 exit 0。
2. 單元測試新增且通過：羅盤方位投影（玩家朝北時北標在中央；目標在背後時隱藏；
   ±110° 邊界漸隱值）；距離格式化（999→'999m'、1500→'1.5km'）；傳送資格
   （聖所未完成=不可、完成=可、廣場恆可）；發現標記 round-trip 存檔。
3. `e2e/qol.spec.ts`（新）：新遊戲（跳過開場）→ 羅盤條可見且含 ≥4 個地標圖示 →
   teleport 至南聖所區域 → 發現標記增加 → 以 debug 完成聖所①（新增
   `debugCompletePuzzle(id)` dev 鉤子）→ fanfare 後能力圖示點亮 → 開暫停選單傳送頁 →
   鍵盤選「靜滯廣場」確認 → poll 位置回到廣場 warpPoint ±2 單位 → 重整 → 發現/完成
   狀態保留。console 零 error + 截圖。
4. 既有七條 e2e 全綠（主控執行）；art/world 預算斷言不變（羅盤 DOM 不計 drawCalls，
   但 fanfare 粒子計入——預算仍須過）。
5. 傳送頁鍵盤可操作性：e2e 中全程僅鍵盤（Tab/方向/Enter）完成傳送。
6. `rg '選單|傳送|羅盤' src/ui --glob '!src/ui/i18n/**'` 零命中（i18n 紀律維持）。

## REPORT（回報格式——完成後照填，寫入 docs/tasks/M8b-report.md）
```
## REPORT
- 結論: PASS / FAIL / BLOCKED + 一句話
- 產出: <絕對路徑>:<行號或行號範圍>
- 驗證證據: 指令 + 關鍵輸出行（每項 ≤5 行）
- 未解決事項與風險: 「無」或條列
```
