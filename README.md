# NEON ECHO

NEON ECHO is a browser-based third-person 3D exploration game set in a silent synthwave city.

## Commands

- `npm install` — install the pinned dependencies.
- `npm run dev` — start the Vite development server.
- `npm run build` — type-check and create a production build.
- `npm run preview` — serve the production build locally.
- `npm run lint` — run ESLint.
- `npm run typecheck` — run strict TypeScript checks without emitting files.
- `npm test` — run the Vitest unit suite once.
- `npm run e2e` — start Vite and run the Chromium Playwright smoke test.

## 操作說明

- 移動：`W` / `A` / `S` / `D`
- 跳躍：`Space`（取得能力後可二段跳）
- 衝刺：`Shift`（需先取得衝刺能力）
- 滑翔：空中按住 `Space`（需先取得滑翔能力）
- 互動／閱讀碑文：`E`
- 暫停選單：`Esc`
- 視角：移動滑鼠；按一下遊戲畫面可鎖定游標

## 如何遊玩

從停滯廣場向南探索，收集殘響碎片、閱讀記憶碑文，並完成三座聖所的環境謎題以解鎖衝刺、二段跳和滑翔。利用新能力前往東、西、北區，最終進入北方流明裂谷的城市核心。

遊戲包含兩種結局。當碎片與能力達到核心要求後，核心會給出最後抉擇；若想看見另一個結局，可在選擇前保留進度，或開始新的探索。
